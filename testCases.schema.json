{
  "$id": "https://mfulton26.github.io/adventofcode/testCases.schema.json",
  "$schema": "http://json-schema.org/schema#",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "name": { "type": "string", "description": "foo" },
      "functionName": { "type": "string", "default": "solve" },
      "input": { "type": ["string", "array"], "items": { "type": "string" } },
      "options": {},
      "args": {
        "type": "array",
        "description": "An entire array of arguments to pass into the solver or function specified by `functionName`."
      },
      "expected": {}
    },
    "required": ["expected"],
    "if": { "required": ["input"] },
    "then": { "not": { "required": ["args"] } },
    "else": { "required": ["args"], "not": { "required": ["options"] } }
  }
}
